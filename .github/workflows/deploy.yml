name: Deploy to TimeWeb

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4
        with:
          server: vh466.timeweb.ru
          username: ci026152
          password: N?sp1!N574cT
          local-dir: ./
          server-dir: /public_html/
          exclude: |
            .git/**
            .github/**
            .gitignore
            README.md
